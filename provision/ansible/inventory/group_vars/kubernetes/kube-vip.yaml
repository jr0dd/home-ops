---
# The interface on the host kube-vip should attach to
kubevip_interface: ens33

kubevip_bgp_peer_ip: 10.10.0.1

kubevip_address: 10.10.4.5

kubevip_bgp_as_number: 64555

kubevip_bgp_peer_as_number: 64560
