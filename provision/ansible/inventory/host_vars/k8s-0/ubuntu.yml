---
apt_install_packages:
  - avahi-daemon
  - samba
  - samba-vfs-modules
  - zsh

managed_groups:
  - name: home_users
    gid: 1042

managed_users:
  - name: j_r0dd
    password: "{{ j_r0dd_passwd }}"
    uid: 2000
    shell: /bin/zsh
    group: home_users
  - name: waleska
    password: "{{ waleska_passwd }}"
    uid: 2001
    group: home_users
  - name: nathan
    password: "{{ nathan_passwd }}"
    uid: 2002
    group: home_users
