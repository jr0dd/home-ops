---
samba_users:
  - name: j_r0dd
    password: "{{ j_r0dd_passwd }}"

samba_shares: |-
  [ansible]
    comment = Ansible
    path = /deadpool/ansible
    public = no
    read only = no
    valid users= j_r0dd
    force group = home_users
