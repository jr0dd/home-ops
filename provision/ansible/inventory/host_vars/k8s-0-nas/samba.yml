---
# Samba users to add. Must have existing system accounts.
samba_enabled: true
samba_users:
  - name: j_r0dd
    password: "{{ j_r0dd_passwd }}"
  - name: waleska
    password: "{{ waleska_passwd }}"
  - name: nathan
    password: "{{ nathan_passwd }}"
