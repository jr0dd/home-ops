---
all:
  children:
    master-nodes:
      hosts:
        k8s-0:
          ansible_host: 10.10.0.10
          ansible_user: ubuntu
    worker-nodes:
      hosts:
    storage-nodes:
      hosts:
        k8s-0:
