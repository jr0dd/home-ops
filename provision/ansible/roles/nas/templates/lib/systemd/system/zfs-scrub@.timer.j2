# {{ ansible_managed }}
[Unit]
Description=zpool scrub on %i

[Timer]
OnCalendar={{ scrub_pools_timer.on_calendar | default('*-*-1,15 01:00:00') }}
AccuracySec={{ scrub_pools_timer.accuracy | default('1h') }}
Persistent=true

[Install]
WantedBy=multi-user.target
