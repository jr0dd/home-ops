---
- name: k3s | configure killall service
  ansible.builtin.template:
    src: k3s-killall.service.j2
    dest: /etc/systemd/system/k3s-killall.service
    mode: 0644

- name: k3s | enable killall service
  ansible.builtin.systemd:
    name: k3s-killall
    daemon_reload: true
    enabled: true
