---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: minio
  namespace: minio
spec:
  storageClassName: openebs-zfspv
  volumeName: minio
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 40Gi
