---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: media
  labels:
    velero.io/exclude-from-backup: "true"
spec:
  storageClassName: openebs-zfspv
  volumeName: media
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
