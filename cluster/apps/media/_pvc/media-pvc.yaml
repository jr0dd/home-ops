---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media
  namespace: media
spec:
  storageClassName: openebs-zfspv
  volumeName: media
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
