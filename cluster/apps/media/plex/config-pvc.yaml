---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plex
  namespace: media
spec:
  storageClassName: openebs-zfspv
  volumeName: plex
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi