---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plex-config
  namespace: media
  labels:
    k10.kasten.io/backup: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
  storageClassName: openebs-zfspv-ssd
