---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plex-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 150Gi
  storageClassName: openebs-zfspv-ssd
