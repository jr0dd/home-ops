---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: lidarr-config
  namespace: media
  labels:
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: openebs-zfspv-ssd
