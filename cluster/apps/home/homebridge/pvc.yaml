---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: homebridge-config
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi
  storageClassName: openebs-zfspv-ssd
