---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: redis
  namespace: databases
spec:
  storageClassName: openebs-zfspv
  volumeName: redis
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
