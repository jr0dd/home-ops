---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql
  namespace: databases
spec:
  storageClassName: openebs-zfspv
  volumeName: postgresql
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
