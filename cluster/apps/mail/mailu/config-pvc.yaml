---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mailu
  namespace: mail
spec:
  storageClassName: openebs-zfspv
  volumeName: mailu
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
