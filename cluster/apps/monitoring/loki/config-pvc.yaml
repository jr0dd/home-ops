---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki
  namespace: monitoring
spec:
  storageClassName: openebs-zfspv
  volumeName: loki
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
