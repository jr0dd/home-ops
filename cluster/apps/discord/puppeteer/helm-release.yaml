---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: puppeteer
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: puppeteer
      version: 1.1.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/puppeteer
      tag: v14.0.0@sha256:372c35f38cda63ce5bb26408284e7feaee0926392543d6dd8711efd85d3a3059
      pullPolicy: Always
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: puppeteer
    env:
      TZ: "America/New_York"
    chromium:
      args:
        - '--disable-gpu'
        - '--disable-software-rasterizer'
        - '--no-zygote'
        - '--disable-features=IsolateOrigins,site-per-process'
        - '--disable-site-isolation-trials'
        - '--disable-web-security'
    service:
      main:
        enabled: true
        ports:
          http:
            port: 4000
    persistence:
      config:
        enabled: false
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
        hosts:
          - host: &host "puppeteer.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: puppeteer-tls
            hosts:
              - *host
