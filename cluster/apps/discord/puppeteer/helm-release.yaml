---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: puppeteer
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: puppeteer
      version: 1.1.1
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/puppeteer
      tag: v13.7.0@sha256:052f0c24739887b2d1f2092c7d4bfd1add1ef3fde53224e326b71277809fafc6
      pullPolicy: Always
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: puppeteer
    env:
      TZ: "America/New_York"
    chromium:
      args:
        - '--disable-gpu'
        - '--disable-software-rasterizer'
        - '--no-zygote'
        - '--disable-features=IsolateOrigins,site-per-process'
        - '--disable-site-isolation-trials'
        - '--disable-web-security'
    service:
      main:
        enabled: true
        ports:
          http:
            port: 4000
    persistence:
      config:
        enabled: false
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
        hosts:
          - host: &host "puppeteer.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: puppeteer-tls
            hosts:
              - *host
