---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: puppeteer
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: puppeteer
      version: 1.1.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/puppeteer
      tag: v14.1.0@sha256:c8b285db6f5aa871328f29dcfa70bbe44756ffade6c074b9a1d8a20fea9da608
      pullPolicy: Always
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: puppeteer
    env:
      TZ: "America/New_York"
    chromium:
      args:
        - '--disable-gpu'
        - '--disable-software-rasterizer'
        - '--no-zygote'
        - '--disable-features=IsolateOrigins,site-per-process'
        - '--disable-site-isolation-trials'
        - '--disable-web-security'
    service:
      main:
        enabled: true
        ports:
          http:
            port: 4000
    persistence:
      config:
        enabled: false
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
        hosts:
          - host: &host "puppeteer.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: puppeteer-tls
            hosts:
              - *host
