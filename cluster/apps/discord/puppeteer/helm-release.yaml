---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: puppeteer
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: /charts/kah-common/
      sourceRef:
        kind: GitRepository
        name: home-ops
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/puppeteer
      tag: v13.3.1@sha256:3b58187202dcfcde6740d794291f5b05913e2d7921ed3f32d21fd80fad263b2f # {"$imagepolicy": "flux-system:puppeteer-ghcr-io:tag"}
      pullPolicy: Always
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: puppeteer
    env:
      TZ: "America/New_York"
    service:
      main:
        enabled: true
        ports:
          http:
            port: 4000
    persistence:
      config:
        enabled: false
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        add:
          - SYS_ADMIN
    ingress:
      main:
        enabled: false
