apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: whalestream-bot
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: /charts/kah-common/
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/whalestream-bot
      tag: v0.2.10 # {"$imagepolicy": "flux-system:whalestream-ghcr-io:tag"}
      pullPolicy: Always
    imagePullSecrets:
      - name: ghcr-io-creds
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: whalestream-bot
    env:
      TZ: "America/New_York"
    envFrom:
      - secretRef:
          name: whalestream-tokens
    service:
      main:
        enabled: true
        ports:
          http:
            port: 3000
    ingress:
      main:
        enabled: true
        ingressClassName: nginx
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          external-dns/is-public: "true"
          external-dns.alpha.kubernetes.io/target: "ipv4.${SECRET_DOMAIN_1}"
        hosts:
          - host: "ws.${SECRET_DOMAIN_1}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: whalestream-tls
            hosts:
              - "ws.${SECRET_DOMAIN_1}"
    podAnnotations:
      secret.reloader.stakater.com/reload: "whalestream-tokens"
