---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wyoa-bot
  namespace: discord
spec:
  interval: 5m
  chart:
    spec:
      chart: /charts/kah-common/
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  values:
    image:
      repository: ghcr.io/jr0dd/wyoa-bot
      tag: v0.2.11
      pullPolicy: Always
    controller:
      enabled: true
      replicas: 1
      strategy: RollingUpdate
    nameOverride: wyoa-bot
    env:
      TZ: "America/New_York"
    envFrom:
      - secretRef:
          name: wyoa
    service:
      main:
        ports:
          http:
            port: 4242
    ingress:
      main:
        enabled: true
        ingressClassName: traefik
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          traefik.ingress.kubernetes.io/router.entrypoints: websecure
          traefik.ingress.kubernetes.io/router.middlewares: networking-cloudflare-ips@kubernetescrd
          external-dns/is-public: "true"
          external-dns.alpha.kubernetes.io/target: "ipv4.${BOT_DOMAIN}"
        hosts:
          - host: "bot.${BOT_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - secretName: wyoa-tls
            hosts:
              - "bot.${BOT_DOMAIN}"
    podAnnotations:
      secret.reloader.stakater.com/reload: "wyoa"
