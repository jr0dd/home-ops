---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: hdhomerun
  namespace: networking
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`hdhomerun.${SECRET_DOMAIN}`)
    kind: Rule
    services:
    - name: hdhomerun
      port: 80
    middlewares:
    - name: rfc1918-ips
  tls:
    secretName: "${SECRET_DOMAIN/./-}-tls"
---
kind: Service
apiVersion: v1
metadata:
  name: hdhomerun
  namespace: networking
spec:
  type: ExternalName
  externalName: "hdhomerun.${SECRET_DOMAIN}"