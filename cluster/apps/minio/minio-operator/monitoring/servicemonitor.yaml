---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: minio
  labels:
    v1.min.io/tenant: tenant0
spec:
  jobLabel: minio
  selector:
    matchLabels:
      v1.min.io/tenant: tenant0
  namespaceSelector:
    matchNames:
      - minio
  endpoints:
    - port: https-minio
      interval: 30s
      scrapeTimeout: 10s
      path: /minio/v2/metrics/cluster
      scheme: https
    - port: https-minio
      interval: 30s
      scrapeTimeout: 10s
      path: /minio/v2/metrics/node
      scheme: https
