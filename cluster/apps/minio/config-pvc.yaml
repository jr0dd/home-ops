---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: minio-config-v1
  namespace: minio
spec:
  volumeName: minio
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: openebs-zfspv-rust
